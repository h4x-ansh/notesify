# Absolute URL of the local Express API (server.js). Baked into the static
# build at `next build` time (this is a static export - see next.config.mjs
# - so there's no server at runtime to read this from). Only override if
# Electron is configured to run the backend on a non-default port.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4500
